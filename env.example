# StaticForms.xyz API Configuration
# Get your API key from https://www.staticforms.xyz
# Copy this file to .env and replace the sample values with your actual API key

# Required: Your StaticForms API key
# Format: sf_your_api_key_here
PUBLIC_STATICFORMS_API_KEY=sf_your_api_key_here

# Optional: URL to redirect to after successful form submission
# Leave empty to show success message on the same page
PUBLIC_STATICFORMS_REDIRECT_URL=/success

# Content Provider Configuration
# Choose between 'markdown' (default) or 'sanity'
# Markdown uses Astro Content Collections (files in src/content/posts/)
# Sanity uses Sanity CMS (configured below)
PUBLIC_CONTENT_PROVIDER=markdown

# Sanity CMS Configuration
# Only required if PUBLIC_CONTENT_PROVIDER=sanity
# Get your project ID from https://sanity.io/manage

# Required for Sanity: Your Sanity project ID
PUBLIC_SANITY_PROJECT_ID=your_project_id_here

# Optional: Sanity dataset (defaults to 'production')
PUBLIC_SANITY_DATASET=production

# Optional: Sanity API version (defaults to '2024-01-01')
# Use the latest version from https://www.sanity.io/docs/js-client#version
PUBLIC_SANITY_API_VERSION=2024-01-01

# Optional: Use CDN for Sanity queries (defaults to true)
# Set to 'false' to always fetch fresh data
PUBLIC_SANITY_USE_CDN=true

# Optional: Sanity API token (server-side only, not exposed to client)
# Only needed for private datasets or write operations
# Get from https://sanity.io/manage
SANITY_API_TOKEN=
